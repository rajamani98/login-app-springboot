server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    password: 4545
    url: jdbc:postgresql://localhost:5432/login-management
    username: postgres
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: springboot49@gmail.com
    password: dsuwrarorawhrmgt

    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000



